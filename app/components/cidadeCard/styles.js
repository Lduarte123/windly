import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: {
        flexDirection: "row",
        alignItems: "center",
        backgroundColor: "#fff",
        marginHorizontal: 24,
        marginBottom: 12,
        borderRadius: 12,
        padding: 16,
        elevation: 1,
        justifyContent: "space-between",
    },
    text: {
        color: "#E53935", 
        fontSize: 18, 
        marginLeft: 12,
    },
})